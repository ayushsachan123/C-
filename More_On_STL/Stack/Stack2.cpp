#include<bits/stdc++.h>
using namespace std;

int main(){

    stack<int>st;

    //cout<<st.top(); throw an error because stack is empty
    //so a;ways write the check function
  
  if(!st.empty()){
    cout<<st.top();
  }

}

